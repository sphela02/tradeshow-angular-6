import { EventUserFilterPipe } from './event-user-filter.pipe';

describe('EventUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EventUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

import { AttendeeFieldsFilterPipe } from './attendee-fields-filter.pipe';

describe('AttendeeFieldsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AttendeeFieldsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

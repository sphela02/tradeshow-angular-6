import { OrganizerFieldsFilterPipe } from './organizer-fields-filter.pipe';

describe('OrganizerFieldsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrganizerFieldsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
